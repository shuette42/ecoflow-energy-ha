# Runtime dependencies (must match manifest.json requirements)
paho-mqtt>=2.1.0
protobuf>=4.21.0
cryptography>=42.0.0

# Test framework - let pytest-homeassistant-custom-component drive
# compatible pytest/pytest-asyncio versions to avoid conflicts
pytest-homeassistant-custom-component>=0.13.0
